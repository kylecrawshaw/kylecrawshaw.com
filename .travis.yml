language: python
python: 2.7

install:
  - "pip install lektor"

script:
  - "lektor build"

deploy:
  provider: script
  script: "lektor deploy s3"
  on:
    branch: master

cache:
  directories:
    - $HOME/.cache/pip
    - $HOME/.cache/lektor/builds
